VITE_API_URL=/api/v1
VITE_RAZORPAY_KEY_ID=your_razorpay_key_id

# Local development only (loaded during `npm run dev`, NOT during `npm run build`)
# Create .env.development with:
# VITE_API_URL=http://localhost:5000/api/v1
